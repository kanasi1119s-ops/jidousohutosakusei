# デバッグログ（3ラウンド）

## Round 1（観点: 機能検証）

自動テスト（Vitest 8件・Playwright E2E 12件）、`tsc --noEmit`、`npm run build` を実行。

| # | 問題 | 再現手順 | 重大度 | 対応 | 修正コミット | 再検証結果 |
|---|---|---|---|---|---|---|
| 1 | ユニットテストの期待値誤り。時給1200円×週20時間×4.345週×12ヶ月＝約125.1万円は130万円の壁を「下回る」のに、テストは「超える(over)」を期待していた | `npm test` を実行 | 中 | テストの期待値を `under` に修正（実装側は正しかった） | 本コミット内（`src/calc.test.ts`） | 再実行で8件全て成功 |

lint/型チェック/ビルドは初回から成功。E2E 12件（デスクトップ/モバイル各6件）も初回成功。

## Round 2（観点: 異常系・境界値・表示・オフライン・文字化け）

追加した `e2e/round2-edge-cases.spec.ts`（巨大値・境界値・空欄・保存復元・PC幅・オフライン）で検証。

| # | 問題 | 再現手順 | 重大度 | 対応 | 修正コミット | 再検証結果 |
|---|---|---|---|---|---|---|
| 2 | **オフラインでのリロードでアプリが真っ白になる（重大）**。Service Worker の install 時に静的なファイル一覧のみを事前キャッシュしており、Viteがハッシュ付きで出力するJS/CSSバンドル（`assets/index-xxxx.js`等）がキャッシュされず、オフライン時に読み込めなかった | ネットワーク切断後にページをリロード | 高 | `public/sw.js` の install ハンドラで、ビルド済み `index.html` を読み込んでそこから実際の `assets/*.js`・`assets/*.css` の参照先を動的に抽出し `cache.addAll` するよう変更 | 本コミット内（`public/sw.js`） | 再検証: サーバーを実際に停止した状態でリロードしても表示・入力・計算が正常動作することを確認（下記詳細） |
| 3 | 修正後もなお、**サーバーを完全に落とした状態でのリロード**でJS/CSSの読み込みが `net::ERR_FAILED` になるケースが残存（重大）。原因調査の結果、`type="module"` のスクリプトはHTML仕様上つねに `mode: "cors"` でフェッチされ、かつページの明示的な `reload()` によって発行されるリクエストはキャッシュを迂回する扱いになるため、`caches.match(event.request)` に **Requestオブジェクトのまま**渡すと、`cache.keys()` には同一URLのキーが存在するにもかかわらずマッチしないことがある（Chromiumの実装挙動）ことを特定した | Playwrightでサービスワーカーのコンソールログを取得しつつ、実際にプレビューサーバーのプロセスを停止した状態で `page.reload()` を実行して再現 | 高 | `public/sw.js` の fetch ハンドラで `cache.match(event.request.url)` のようにURL文字列で照合するよう変更し、Requestオブジェクトの`mode`起因のマッチ失敗を回避。あわせて、Viteがデフォルトで付与する `crossorigin` 属性が不要な同一オリジン構成のため、ビルド後の `<script>/<link>` から除去するカスタムVite プラグイン（`stripCrossorigin`）を追加した | 本コミット内（`public/sw.js`, `vite.config.ts`） | Playwrightで実際にサーバープロセスを停止した状態でリロードし、`response.fromServiceWorker() === true` かつ画面（`h1`・フォーム）が正常表示されることを確認。`e2e/round2-edge-cases.spec.ts` の「オフライン」テストとして自動テスト化 |
| 4 | （確認のみ・問題なし）巨大な入力値（時給1億円）でもクラッシュせず表示 | E2Eテスト | - | 対応不要 | - | 成功 |
| 5 | （確認のみ・問題なし）週168時間超の入力、空欄入力はいずれもエラーメッセージが表示されクラッシュしない | E2Eテスト | - | 対応不要 | - | 成功 |
| 6 | （確認のみ・問題なし）入力値はリロード後も `localStorage` から復元される | E2Eテスト | - | 対応不要 | - | 成功 |
| 7 | （確認のみ・問題なし）スマホ幅(375px)・PC幅(1280px)いずれも横スクロールが発生しない | E2Eテスト | - | 対応不要 | - | 成功 |

**文字化けチェック（Round 2内）**:
- CSVダウンロード: 先頭3バイトが `EF BB BF`（BOM）であることをE2Eテストで確認済み（`e2e/basic.spec.ts`）
- 日本語ヘッダー「しきい値名」等がCSV本文に正しく含まれることを確認
- 画面表示（見出し・フォームラベル・免責事項）はすべて日本語で正しく表示されることをE2Eで確認

問題2・3はいずれも重大度「高」のため、本ラウンド内で修正・再検証まで完了（3-3節の運用ルールに準拠）。

## Round 3（観点: クリーン環境・回帰）

`git write-tree` + `git archive` で現在のコミット対象ツリーを別ディレクトリに展開し、
「新規clone直後」相当の状態を再現。README記載の手順のみで検証した。

| # | 手順 | 結果 |
|---|---|---|
| 1 | `npm install` | 成功（脆弱性件数はSecurityフェーズで別途報告） |
| 2 | `npm run check`（tsc --noEmit） | 成功 |
| 3 | `npm test`（Vitest） | 8件成功 |
| 4 | `npm run build` | 成功、`dist/` にハッシュ付きJS/CSS・manifest・sw.js・iconが出力されることを確認 |
| 5 | `dist/` を `python3 -m http.server` で配信し、実際にHTTPアクセスできることを確認 | `index.html`・`sw.js`・`manifest.webmanifest` いずれも200応答 |
| 6 | `npx playwright test`（Round1・Round2で追加した24件全て） | 24件全て成功。Round1・2の修正によるデグレードなし |

README手順の不備・回帰は検出されなかった。

## まとめ

- Round 3終了時点で重大度「高」の問題は **0件**（Round 2で検出した2件はいずれも同ラウンド内で修正・再検証済み）
- MVP完成の判定基準を満たしている
