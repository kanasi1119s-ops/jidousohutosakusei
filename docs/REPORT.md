# 日次レポート: 2026-09-23

## ⚠ 実行上の問題（最上部に明記）

- 本セッションのGitHub操作権限は**リポジトリ単位**（`kanasi1119s-ops/jidousohutosakusei`
  1件のみ）に限定されており、`kanasi1119s-ops`配下への**新規リポジトリの作成**、
  引き継ぎ用リポジトリ`orchestrator-log`の作成・参照、および組織配下の既存リポジトリ
  一覧の取得を行うことができなかった（運用指示書 第1章7. 記載の「リポジトリ単位でしか
  操作できない」旨のエラーに該当）。
- そのため運用指示書の想定（日次で新規リポジトリを作成し、`orchestrator-log`で
  引き継ぎを管理する）は今回実行できず、**フォールバック手順**として、付与された
  唯一のリポジトリ`jidousohutosakusei`のブランチ`claude/elegant-brown-8r26w8`上に
  今回のプロジェクトを直接構築し、コミット・pushした。あわせてビルド済み静的ファイルを
  ZIPにまとめ、セッションの送付機能でユーザーに直接送付する。
- Phase -1（前回サイクルの引き継ぎ確認）は、上記の理由により`orchestrator-log`の
  `BACKLOG.md`等を参照できず実施不可。今回は「新規着手」として扱った。
- 別アカウントでの作成など、権限スコープの回避策は取っていない。

## 本日のプロジェクト

1. **ふるさと納税 控除上限額シミュレーター** - サイト形態: W1（静的サイト・PWA）
   - スコア: 23/25（需要5・実装難易度4・法的リスクの低さ5・差別化4・運用コストの低さ5）
   - 状態: **MVP完成**（デバッグ3ラウンド完了、重大度「高」の残件0）

## マーケットリサーチ結果サマリ

- 採用案: ふるさと納税の控除上限額シミュレーター。9〜12月にかけて検索需要が高まる
  定番テーマであり、既存の各ポータルサイト(さとふる・ふるなび・ふるさとチョイス等)の
  簡易シミュレーターは、入力データをサーバーに送信する前提のものが多い。「クラウドに
  預けたくない」「登録不要」というニーズに応える、ブラウザ内完結・外部送信ゼロの
  シミュレーターとして差別化した。
- 却下案: 今回は既存リポジトリが本プロジェクトのみ（新規作成不可のため機能重複調査の
  対象も無し）であり、他の候補案の詳細な比較検討は実施していない（次サイクルへの
  引き継ぎ課題とする）。
- 計算式の根拠: 総務省「ふるさと納税のしくみ｜税金の控除について」
  （控除上限額 ≒ 住民税所得割額 × 20% ÷ (90% − 所得税率 × 1.021) + 2,000円）。

## 実装内容

- リポジトリ: `kanasi1119s-ops/jidousohutosakusei`（private）、ブランチ
  `claude/elegant-brown-8r26w8`
- 主要機能: 年収・家族構成からの控除上限額計算、localStorageへの保存・一覧・削除、
  JSON(バックアップ往復)/CSV(BOM付きUTF-8)のエクスポート・インポート、PWA(オフライン対応)、
  レスポンシブ(375px〜1280px+)、外部通信ゼロを強制するCSP。
- テスト: ユニット19件・E2E 18件(chromium/mobile) — **すべて成功**。
- 既知の制約・未実装機能:
  - 保存記録の「メモ」欄はデータモデル上は存在するが、入力UIは未実装（常に空文字）。
  - 社会保険料控除は年収の15%という簡易仮定であり、医療費控除・iDeCo・住宅ローン控除・
    調整控除は計算に含まれていない（免責事項・DESIGN.mdに明記済み）。
  - 個人事業主向けの入力モードは未対応（給与所得者を想定）。

## デバッグ3ラウンド結果

| ラウンド | 検出数(高/中/低) | 修正数 | 持ち越し |
|---|---|---|---|
| Round 1 | 1 / 0 / 0 | 1 | 0 |
| Round 2 | 1 / 1 / 0 | 2 | 0 |
| Round 3 | 0 / 0 / 0 | 0（回帰なしを確認） | 0 |

- 詳細: `docs/DEBUG_LOG.md`
- Round1で検出: 年収0円のとき控除上限額が誤って2,000円になる計算バグ → 修正・再検証済み。
- Round2で検出: 保存ボタン連続クリックで結果表示が消えるUXバグ／オフラインE2Eの
  タイミング競合 → いずれも修正・再検証済み。
- Round3: クリーンclone→install→check→build→静的配信の確認、および全回帰テストで
  重大度「高」は0件。

## セキュリティ・法務チェック結果

### 確認済み項目
- `npm audit`: 初回検出の6件(moderate〜critical、開発サーバー限定)をvite/vitest/
  vite-plugin-pwa更新により**0件**に解消。更新後も全テスト成功を再確認済み。
- 秘密情報混入チェック: 全追跡ファイルを走査し該当なし。
- OSSライセンス: 実行時依存はゼロ（`--production`で`UNLICENSED`＝自プロジェクトのみ）。
  開発時依存にもGPL/AGPL等の強いコピーレフトは0件。詳細は`docs/THIRD_PARTY_LICENSES.md`。
- XSS/入力データの扱い: ユーザー自由入力を直接HTML化する経路は現状無し。CSVインポートは
  形式不正時に例外で拒否し既存データを破壊しない。
- CSP設定: `default-src 'self'`ベースの厳格なCSPを追加し、外部通信ゼロをブラウザレベルで強制。
- 法務: 第三者コンテンツ不使用、スクレイピング無し、個人情報は外部送信されない設計、
  免責事項・出典明記、利用規約案・プライバシーポリシー案を用意（`docs/legal/`）。
  誇大な効果保証表現は使用していない。

### 人間の確認が必要な項目（★重要）
- ★ プライバシーポリシー・利用規約の最終文言確認と設置。
- ★ `LICENSE`のライセンス方針（OSS化してMIT等にするか、独自ライセンスとするか）の最終決定。
- ★ 将来有料版・広告を追加する場合の特定商取引法表記の追加。

## 文字化けチェック結果

- 全テキストファイルのUTF-8検証: **OK**（追跡ファイル36件、置換文字(U+FFFD)や既知の
  文字化けパターンを含むファイルは0件）。作業中に1件、ドラフト文書内へ誤って挿入された
  置換文字(U+FFFD)を検出し、その場で修正済み（`docs/legal/PRIVACY_POLICY_DRAFT.md`）。
- CSVエクスポート（BOM付きUTF-8）: **OK**。PlaywrightE2Eで実際にダウンロードした
  CSVファイルの先頭3バイトが`EF BB BF`であること、日本語ラベルが正しく含まれることを確認済み。

## 公開に向けて人間がすべきこと

- [ ] リポジトリの public化の判断
- [ ] GitHub Pages の有効化（Settings→Pages→Source: GitHub Actions）と、
      `.github/workflows/deploy-pages.yml`のActionsタブからの手動実行
- [ ] 販売／決済アカウントの接続（現時点では無料提供のため不要。将来有料化する場合のみ）
- [ ] ライセンスキー署名用の秘密鍵生成・保管（現時点では有料版なしのため不要）
- [ ] ドメイン取得（GitHub Pagesのデフォルトドメインで運用する場合は不要）
- [ ] プライバシーポリシー・利用規約・`LICENSE`の最終確認（`docs/legal/`参照）
- [ ] （その他）引き継ぎ用リポジトリ`orchestrator-log`が必要な場合は、人間が
      GitHub上で作成し、このセッションまたは次回サイクルに権限を付与すること

## マーケ素材

- LP草稿: `docs/marketing/LP_COPY.md`
- キャッチコピー案: `docs/marketing/CATCHCOPY.md`
- SNS告知文: `docs/marketing/SNS_POSTS.md`

## 翌サイクルへの引き継ぎ

未完了タスク（優先度順）:
1. GitHub操作権限が単一リポジトリに限定されている問題の解消（`orchestrator-log`
   および新規リポジトリ作成に必要な権限を、人間が次回サイクル開始前に確認・付与すること）。
2. 保存記録の「メモ」入力UIの追加（現状データモデルのみ存在）。
3. 詳細モード（住民税所得割額の直接入力、iDeCo・医療費控除等の追加入力）の検討。

次に検討すべきサイト候補（バックログ、サイト形態の案つき）:
- 年末調整の必要書類チェックリスト＆記入補助ツール（W1、11〜12月に需要増）
- 個人事業主向け簡易帳簿・確定申告の期日カウントダウン＆チェックリスト（W1）
- インボイス制度対応の請求書テンプレート＋消費税計算ツール（W1、ブラウザ内PDF生成）
