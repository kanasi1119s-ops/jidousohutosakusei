# デバッグログ

## Round 1（観点: 機能検証）

| # | 問題 | 再現手順 | 重大度 | 対応 | 修正コミット | 再検証結果 |
|---|---|---|---|---|---|---|
| 1 | 年収0円で計算すると、控除上限額が0円ではなく2,000円になる | `simulate({ annualIncome: 0, familyType: 'single' })` を実行 | 高 | 住民税所得割額が0円のときは常に上限額も0円になるよう `calc.ts` の `rawLimit` 計算を修正（自己負担2,000円の加算は住民税所得割額>0の場合のみ行う） | 本ブランチのコミット（calc.ts修正） | ユニットテスト`never returns a negative donation limit`が成功。再検証OK |

その他: ユニットテスト14件、lint、typecheck、`vite build`、Playwright E2E 10件（chromium/mobile）はすべて成功。

## Round 2（観点: 異常系・境界値・表示・文字化け）

追加観点: 負の年収、超巨大な年収、二重操作（連続クリック）、オフライン動作、無効なJSONインポート、PC幅(1280px)表示、日本語文字列のCSV/JSONエスケープ。

| # | 問題 | 再現手順 | 重大度 | 対応 | 修正コミット | 再検証結果 |
|---|---|---|---|---|---|---|
| 2 | 「この結果を保存する」ボタンを連続2回クリックすると、1回目の保存後に画面全体が再描画され結果カードが非表示に戻るため、2回目のクリック対象のボタンが見つからずタイムアウトする（実運用でも「保存後に結果が消えて再計算が必要になる」というUXバグ） | E2E: 計算→保存→保存 を連続実行 | 高 | `main.ts`の保存・削除・インポート・全削除の各ハンドラで、フォームと結果カードを含む全体`render()`を呼ぶのをやめ、記録一覧部分のみを再描画する`refreshRecordsArea()`に変更。これにより計算結果が保持されたまま何度でも保存できる | 本ブランチのコミット（main.ts修正） | E2E「保存ボタンを連続で2回押しても...」がchromium/mobile両方で成功 |
| 3 | オフラインE2Eテストが`mobile`プロジェクトでのみ`net::ERR_INTERNET_DISCONNECTED`で失敗（Service Workerの事前キャッシュ完了前にオフラインへ切り替えていたことによるテスト自体の競合状態） | E2Eを2ブラウザプロジェクトで実行 | 中 | テスト側で`navigator.serviceWorker.ready`の`active`状態を待ってからオフラインに切り替えるよう修正 | 本ブランチのコミット（e2e修正） | chromium/mobile両方で成功 |

観点別の追加確認:
- 負の年収・超巨大な年収（`999999999999`）: `NaN`/`Infinity`にならず、`0`以上の有限値を返すことをユニットテストで確認済み。
- 無効なJSONインポート: 例外を投げ、既存の保存データを破壊しないことを確認済み（`storage.test.ts`）。
- 日本語を含むCSV/JSONの往復（エクスポート→全削除→インポート）でデータ・文字列が保持されることを確認済み。
- PC幅(1280px)・スマホ幅(375px)双方で主要要素が表示されることをE2Eで確認済み。

## Round 3（観点: クリーン環境・回帰・静的配信）

| # | 確認項目 | 結果 |
|---|---|---|
| 1 | リポジトリを新規clone → `npm install` → `npm run check`（lint+typecheck+test） | 成功（下記コマンド実行ログ参照） |
| 2 | `npm run build` でビルド | 成功 |
| 3 | ビルド済み`dist/`を簡易HTTPサーバーで配信し、実際にブラウザ相当（Playwright）で開けるか | 成功。E2E全18件を`vite preview`（ビルド済み資産を配信）に対して実行し成功 |
| 4 | Round1・Round2の修正によって新たに壊れた箇所がないかの回帰 | ユニットテスト19件・E2E18件が全件成功。回帰なし |

重大度「高」は0件（Round1・Round2で検出した2件はいずれもそのラウンド内で修正・再検証済み）。
